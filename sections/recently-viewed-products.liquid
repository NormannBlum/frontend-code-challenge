{% comment %}
  Recently Viewed Products Section

  This section file defines a reusable section for the Shopify Theme Editor
  that allows merchants to configure a block for displaying products
  that the customer has recently viewed.

  It primarily uses a Liquid snippet ('recently-reviewed') to render
  the actual product list and carousel.

  Configuration settings (defined in the schema) are passed to the snippet.
{% endcomment %}

<div class="page-width">
  {#
    Render the 'recently-reviewed' snippet.
    The actual logic for fetching and displaying recently viewed products
    will be handled within this snippet.
    Section settings are passed as arguments to the snippet.
  #}
  {% render 'recently-reviewed',
    title: section.settings.title,
    subtitle: section.settings.subtitle,
    max_products: section.settings.max_products
  %}
</div>

{% schema %}
{
  "name": "Recently Viewed Block",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Überschrift",
      "default": "Kürzlich angesehene Produkte"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Untertitel",
      "default": "Basierend auf deinem Browserverlauf"
    },
    {
      "type": "number",
      "id": "max_products",
      "label": "Max Produkte",
      "default": 4
    }
  ],
  "presets": [
    {
      "name": "Recently Viewed Block",
      "category": "Product"
    }
  ],
  "enabled_on": {
    "templates": ["*"],
    "groups": ["footer"]
  }
}
{% endschema %}